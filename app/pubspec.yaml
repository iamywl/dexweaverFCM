name: dexweaver_fcm
description: DexWeaver FCM QoS Research App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.8.1
  firebase_messaging: ^15.2.1
  cloud_firestore: ^5.6.2
  device_info_plus: ^11.2.0
  battery_plus: ^6.2.0
  connectivity_plus: ^6.1.2
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
